package com.pep.taskmanagement.models.Enums;

public enum TaskStatus {
    // TASK is created not not started yet
    TO_DO,

    // Task in progress
    IN_PROGRESS,

    // Task is done;
    DONE,
    
    // Task is Blocked can't work on that
    BLOCKED

}
